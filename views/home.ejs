<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1 style="text-align: center;">Welcome to the home page</h1>
    <h2 style="text-align: center;">Hello, <%= username %>!</h2> 
    <p><a href="/seller">Sell Ticket</a></p>
    <input type="search" placeholder="Search...">
    <h2>Available Tickets</h2>
<% if (tickets && tickets.length > 0) { %> <!-- Check if tickets is defined and has items -->
    <ul>
        <% tickets.forEach(ticket => { %>
            <li>
                <strong>Source:</strong> <%= ticket.source %>, 
                <strong>Destination:</strong> <%= ticket.destination %>, 
                <strong>Price:</strong> <%= ticket.price %>, 
                <strong>Tickets Available:</strong> <%= ticket.ticket %>, 
                <strong>Comments:</strong> <%= ticket.comments %>
            </li>
        <% }); %>
    </ul>
<% } else { %>
    <p>No tickets available.</p>
<% } %>

</body>
</html>
